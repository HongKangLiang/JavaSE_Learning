package interface_sample;

public interface Jumpping {
    // 在接口中定义方法像下面这样写是可以的，但是因为接口中的方法默认public abstract，所以我们可以省略
    //public abstract void jump();
    //像上面这样写是可以的，但是因为接口中的方法默认public abstract，所以我们可以省略，写成这样
    void jump();
}
